31:         // ?�고 ?�태 ?�별
32:         const inventory = currentProduct.inventory;
33:         const isOutOfStock = inventory != null && inventory <= 0;
34:         const isLowStock = inventory != null && inventory > 0 && inventory <= 5;
35: 
36:         // ?�절???�니�??�고 부족도 ?�니�?추천 불필??        if (!isOutOfStock && !isLowStock) {
37:             return { similarProducts: [], isOutOfStock, isLowStock };
38:         }
39: 
40:         // ?�?� ?�상 계열 분류 ?�수 ?�?�
41:         // ?? colorText가 ?�확???�치?��? ?�더?�도 같�? "계열"?�면 ?�사?�다�??�단?�기 ?�함
42:         const getColorFamily = (product) => {
43:             const text = (product.colorText || product.specifications?.color || product.title || '').toLowerCase();
44: 
45:             if (text.includes('?�이??) || text.includes('?�림') || text.includes('?�이보리') || text.includes('?�닉??)) return 'white';
46:             if (text.includes('그레??) || text.includes('?�쉬') || text.includes('콘크리트') || text.includes('?�모??)) return 'gray';
47:             if (text.includes('브라??) || text.includes('?�크') || text.includes('?�넛') || text.includes('?�드') || text.includes('메이??)) return 'brown';
48:             if (text.includes('베이지') || text.includes('?�드') || text.includes('?�추??)) return 'beige';
49:             if (text.includes('?�크') || text.includes('블랙') || text.includes('차콜')) return 'dark';
50:             return 'other';
51:         };
52: 
53:         const currentColorFamily = getColorFamily(currentProduct);
54:         const currentPatterns = currentProduct.patterns || [];
55:         const currentPrice = currentProduct.price || 0;
